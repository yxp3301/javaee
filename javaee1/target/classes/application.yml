spring:
  datasource:
    url: jdbc:mysql://localhost:3306/javaee?useSSL=false&serverTimezone=UTC
    username: root
    password: yxp330123
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.entity
server:
  port: 8080
  servlet:
    context-path: /app